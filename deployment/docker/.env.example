# Example environment configuration for Context (Docker Compose)
# Copy to deployment/docker/.env and adjust values for your environment

# PostgreSQL
POSTGRES_DB=context_prod
POSTGRES_USER=context
POSTGRES_PASSWORD=change-me
DATABASE_URL=postgresql://context:change-me@postgres:5432/context_prod

# Redis
REDIS_URL=redis://redis:6379/0

# Qdrant
QDRANT_HOST=qdrant
QDRANT_PORT=6333
QDRANT_COLLECTION=context_vectors
QDRANT_VECTOR_SIZE=384
# QDRANT_API_KEY=your-qdrant-api-key

# Ollama (optional)
OLLAMA_BASE_URL=http://ollama:11434

# Server
ENVIRONMENT=production
LOG_LEVEL=INFO
MCP_ENABLED=true
MCP_SERVER_NAME=Context

# Security (optional but recommended)
API_AUTH_ENABLED=true
API_KEY=replace-with-a-secure-random-string

